"use strict"; //treat all JS code as newer version
//nowadays if you not mention then its automatically recognize

// alert(3 + 3); //if you want to run this alert then attach to the html file //in the node js it is not running
//we are using nodejs, not browers

let name = "aniket"
let age = 20.3
let isLoggedIn = true

console.table([name, age, isLoggedIn])

let foo = 42; // foo is now a number
console.log("foo is number: " + foo)

foo = "bar"; // foo is now a string
console.log("foo is string: " + foo);

foo = true; // foo is now a boolean
console.log("foo is boolean: " + foo);

let state1; // null
let state2 = null; // null 

// number
// bigint
// string => ""/''
// boolean
// null => standalone value
// undefiend => 
// symbol => unique

//object

console.log(typeof "aniket")
console.log(typeof null) //object
//null is the object

console.log(typeof undefined); //undefined
